version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: rina-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./frontend/build:/usr/share/nginx/html:ro
      - certbot-data:/etc/letsencrypt:ro
      - certbot-www:/var/www/certbot:ro
    depends_on:
      - backend
    networks:
      - rina-network
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: rina-backend
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://rina_user:${POSTGRES_PASSWORD}@postgres:5432/rina_db
      - JWT_SECRET=${JWT_SECRET}
      - REDIS_URL=redis://redis:6379
      - MINIO_ENDPOINT=minio
      - MINIO_PORT=9000
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
      - S3_BUCKET_NAME=${S3_BUCKET_NAME:-rina-uploads}
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - TMDB_API_KEY=${TMDB_API_KEY}
      - VAPID_PUBLIC_KEY=${VAPID_PUBLIC_KEY}
      - VAPID_PRIVATE_KEY=${VAPID_PRIVATE_KEY}
    # Backend is NOT exposed to host; only accessible via nginx reverse proxy
    expose:
      - "3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - rina-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:16-alpine
    container_name: rina-postgres
    environment:
      - POSTGRES_USER=rina_user
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=rina_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - rina-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rina_user -d rina_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: rina-redis
    networks:
      - rina-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: rina-minio
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ACCESS_KEY}
      - MINIO_ROOT_PASSWORD=${MINIO_SECRET_KEY}
    volumes:
      - minio_data:/data
    # MinIO is NOT exposed to host; only accessible via internal Docker network
    expose:
      - "9000"
      - "9001"
    networks:
      - rina-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  # ─── Coturn (TURN Server for WebRTC) ────────────────────────────
  # In production on AWS Lightsail, run Coturn on a dedicated instance
  # with public IP and open UDP ports. Uncomment below for local testing.
  # coturn:
  #   image: coturn/coturn:latest
  #   container_name: rina-coturn
  #   ports:
  #     - "3478:3478"
  #     - "3478:3478/udp"
  #     - "5349:5349"
  #     - "5349:5349/udp"
  #     - "49152-65535:49152-65535/udp"
  #   environment:
  #     - DETECT_EXTERNAL_IP=yes
  #     - DETECT_RELAY_IP=yes
  #   command: >
  #     -n --log-file=stdout
  #     --listening-port=3478
  #     --tls-listening-port=5349
  #     --realm=${COTURN_REALM:-localhost}
  #     --server-name=rina-coturn
  #     --fingerprint
  #     --lt-cred-mech
  #     --user=maroon:${COTURN_SECRET:-rina2026}
  #     --user=rina:${COTURN_SECRET:-rina2026}
  #     --no-cli
  #     --min-port=49152
  #     --max-port=65535
  #   networks:
  #     - rina-network
  #   restart: unless-stopped

  # certbot:
  #   image: certbot/certbot
  #   container_name: rina-certbot
  #   volumes:
  #     - certbot-data:/etc/letsencrypt
  #     - certbot-www:/var/www/certbot
  #   entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
  #   networks:
  #     - rina-network

volumes:
  postgres_data:
  minio_data:
  certbot-data:
  certbot-www:

networks:
  rina-network:
    driver: bridge
